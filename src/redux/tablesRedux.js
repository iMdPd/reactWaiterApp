/* SELECTORS */
/* ACTIONS */
const createActionName = (actionName) => `app/tables/${actionName}`;

/* ACTION CREATORS */

export const tablesReducer = (statePart = [], action) => {
  switch (action.type) {
    default:
      return statePart;
  }
};
